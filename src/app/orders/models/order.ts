export interface Order {
  flavors: string[];
  milk: string;
  name: string;
  phone: string;
  temp: string;
  notes: string;
  key: string;
}

export interface SMS {
  to: string;
  body: string;
}
